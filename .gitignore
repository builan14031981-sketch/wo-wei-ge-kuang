# 编译产物 / 打包产物
__pycache__/
*.pyc
build/*
!build/installer.nsi
dist/
*.spec

# 发布用的二进制（体积大，不纳入版本库；由打包流程生成）
我为歌狂便携版.zip
我为歌狂安装版.exe

# 二进制（ffmpeg 体积大，不纳入版本库，打包时由 imageio-ffmpeg 提供）
ffmpeg.exe
ffmpeg

# 系统 / 编辑器
.DS_Store
Thumbs.db
*.lnk

# 本地令牌等敏感文件（切勿提交）
*.token
gh_token.txt

# opencode 内部数据
.opencode/

# 运行时生成的数据文件
history.json

# 图标方案预览 / 候选图标 (仅保留 app_icon.ico)
icons_preview/
icon_方案*.ico

